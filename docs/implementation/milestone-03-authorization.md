# M03 implementation authorization

**Status:** Approved for implementation  
**Approval date:** 2026-08-29  
**Milestone:** M03 — In-application catalogue and business settings  
**Approved contract:** `docs/implementation/milestone-03-catalogue-settings.md`

## Authorization

The user explicitly approved implementation of M03 on 2026-08-29 after review of the prepared detailed contract.

Codex is authorized to implement **only** the scope defined by `docs/implementation/milestone-03-catalogue-settings.md`, using current GitHub `main` and all Approved specification/decision records as authority.

This authorization does not permit:

- merge of the M03 pull request without a later explicit user approval;
- implementation of M04 or any later milestone;
- alteration of frozen business, data, architecture, handoff, printing, import/export or retention semantics outside an approved specification amendment.

If the detailed M03 contract conflicts with a later Approved source on `main`, the later Approved source wins and the conflict must be surfaced rather than guessed.

## Required delivery

Implementation proceeds on `codex/m03-catalogue-settings` with an open PR targeting `main`. Completion must include the automated/manual evidence, acceptance traceability and `CODEX_DONE` delivery required by the contract and `AGENTS.md`.
